---
const { href, label, activeClass } = Astro.props;
---

<a
  href={href}
  class={`text-gray-800 transition-colors duration-300 transform dark:text-gray-200  ${activeClass}  mx-1.5 sm:mx-6 capitalize`}
  >{label}</a
>
